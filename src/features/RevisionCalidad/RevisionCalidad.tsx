import React from 'react';
import RevisionCalidadPage from './components/RevisionCalidadPage';

const RevisionCalidad: React.FC = () => {
  return <RevisionCalidadPage />;
};

export default RevisionCalidad;
